<%#-include('./style')%>
<div class="card-body">
    <div class="row">
        <hr>
        <div class="tab-content" id="nav-tabContent">
            <div class="tab-pane fade show active" id="nav-user" role="tabpanel" aria-labelledby="nav-user-tab">
                <div class="row">
                    <table class="table table-striped table-bordered table-hover visible" id="datatable">
                        <thead>
                            <tr>
                                <th>Sr No</th>
                                <th>Collection Name</th>
                                <th>Creator</th>
                                <th>Owner</th>
                                <th>Action</th>
                            </tr>
                        </thead>

                    </table>
                </div>
            </div>


        </div>

    </div>
</div>

<%- include('../partials/datatable-script')%>
<script>
    $(document).ready(()=>{

    })
    const axiosConfig = {
        url:`${config.SERVER_URL}${config.URLS.INFATUATION_LIST}`,
        type: 'GET'
    }
    const columns = [
        {},
        {
            render:(data, type, row, meta) => {
                return row.collectionDoc.length > 0 ? row.collectionDoc[0]?.title : ''
            }
        },
        {
            render:(data, type, row, meta) => {
                return row.collectionDoc.length > 0 ? row.collectionDoc[0]?.title : ''
            }
        }
    ]
    const loadTable = () => initiateDatatable('datatable', axiosConfig)
</script>